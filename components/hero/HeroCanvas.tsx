"use client";

export function HeroCanvas() {
  return (
    <div className="relative w-full aspect-[16/5] rounded-lg sm:rounded-[2rem] overflow-hidden bg-[var(--background-surface)]">
    </div>
  );
}
